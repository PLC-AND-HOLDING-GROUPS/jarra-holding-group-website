server {
    listen 80;
    server_name jarra.et www.jarra.et;

    # Let's Encrypt validation
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # HTTP to HTTPS redirect
    location / {
        return 301 https://$host$request_uri;
    }

    location = /health {
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}

server {
    listen 80 default_server;
    listen 443 ssl default_server;
    server_name _;

    # HTTPS Configuration (Uncomment and configure AFTER obtaining SSL)
    # ssl_certificate /etc/letsencrypt/live/jarra.et/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/jarra.et/privkey.pem;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location = /health {
        return 200 'OK';
        add_header Content-Type text/plain;
    }

    # Allow public access to manifest and static files to avoid 401 console errors
    location /favicon_io/ {
        proxy_pass http://jarra_frontend_prod:3000;
        proxy_set_header Host $host;
    }

    location = /favicon.ico {
        proxy_pass http://jarra_frontend_prod:3000;
        proxy_set_header Host $host;
    }

    location = /site.webmanifest {
        proxy_pass http://jarra_frontend_prod:3000;
        proxy_set_header Host $host;
    }

    location / {
        proxy_pass http://jarra_frontend_prod:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    location ~ ^/api/auth/(login|logout|request-otp|verify-otp|reset-password) {
        proxy_pass http://jarra_backend_prod:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/auth {
        proxy_pass http://jarra_frontend_prod:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api {
        proxy_pass http://jarra_backend_prod:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads {
        proxy_pass http://jarra_backend_prod:4000/uploads;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    client_max_body_size 200M;
}
